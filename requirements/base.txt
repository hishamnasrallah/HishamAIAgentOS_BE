# Core Django
Django==5.0.1
djangorestframework==3.14.0
django-cors-headers==4.3.1
django-filter==24.1
django-environ==0.11.2

# Database
# psycopg2-binary==2.9.9

# Vector database support
pgvector==0.2.5

# Async support
channels==4.0.0
channels-redis==4.2.0
daphne==4.0.0  # ASGI server for WebSocket support
uvicorn[standard]==0.27.0
fastapi==0.109.0

# Celery
celery==5.3.6
redis==5.0.1
django-redis==5.4.0  # Redis cache backend for Django
django-celery-beat>=2.7.0  # Django 5.0 compatible version

# Authentication
djangorestframework-simplejwt==5.3.1
pyotp==2.9.0
qrcode==7.4.2
Pillow>=10.4.0

# AI Platform SDKs
openai==1.10.0
anthropic==0.18.1
google-generativeai==0.3.2
httpx>=0.25.0  # Required for OpenAI library compatibility

# Utilities
python-dotenv==1.0.1
requests==2.31.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-slugify==8.0.1

# Monitoring
prometheus-client==0.19.0
sentry-sdk==1.40.0

# Security
cryptography==42.0.2
hvac==2.1.0

# Data processing (optional - not currently used in codebase)
# pandas>=2.2.2  # Requires C compiler - install separately if needed
# numpy>=1.26.4  # Requires C compiler - install separately if needed

# API documentation
drf-spectacular==0.27.0

# Markdown processing
markdown>=3.4.0
Pygments>=2.15.0  # For code highlighting
PyYAML>=6.0.1  # For parsing YAML frontmatter in documentation

# Date and time
python-dateutil==2.8.2
pytz==2024.1

# Testing
pytest==7.4.4
pytest-django==4.8.0
pytest-cov==4.1.0
pytest-asyncio==0.23.3
pytest-mock==3.12.0
