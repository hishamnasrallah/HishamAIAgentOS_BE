# اختبار نظام الوثائق - Testing Documentation System

**التاريخ:** 06 ديسمبر 2025

---

## 🧪 اختبار النظام

### 1. اختبار Backend API

#### أ. اختبار List Files
```bash
# اختبار endpoint
curl -X GET "http://localhost:8000/api/v1/docs/list_files/?view=tree" \
  -H "Authorization: Bearer YOUR_TOKEN"

# اختبار مع Role filter
curl -X GET "http://localhost:8000/api/v1/docs/list_files/?view=tree&role=Developer" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**النتائج المتوقعة:**
- ✅ 200 OK
- ✅ يحتوي على `files` array
- ✅ يحتوي على `tree` structure
- ✅ عدد الملفات ≈ 172

#### ب. اختبار Get File
```bash
# اختبار جلب ملف
curl -X GET "http://localhost:8000/api/v1/docs/get_file/?path=03_TESTING/QUICK_START_TESTING_GUIDE.md&output_format=html" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**النتائج المتوقعة:**
- ✅ 200 OK
- ✅ يحتوي على `content` أو `html`
- ✅ المحتوى موجود وصحيح

#### ج. اختبار Search
```bash
# اختبار البحث
curl -X GET "http://localhost:8000/api/v1/docs/search/?q=testing&limit=10" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**النتائج المتوقعة:**
- ✅ 200 OK
- ✅ يحتوي على `results` array
- ✅ النتائج ذات صلة بكلمة البحث

---

### 2. اختبار Frontend

#### أ. فتح صفحة `/docs`
1. شغّل Backend (`python manage.py runserver`)
2. شغّل Frontend (`npm run dev`)
3. افتح `http://localhost:5173/docs`

**النتائج المتوقعة:**
- ✅ الصفحة تفتح بدون أخطاء
- ✅ تظهر قائمة الملفات
- ✅ يمكن التنقل بين الملفات
- ✅ لا توجد أخطاء 404 في Console

#### ب. اختبار Tree View
- ✅ الملفات منظمة في tree structure
- ✅ يمكن فتح/إغلاق المجلدات
- ✅ يمكن النقر على الملفات

#### ج. اختبار Topics View
- ✅ الملفات منظمة حسب Topics
- ✅ كل topic يحتوي على ملفاته
- ✅ يمكن التنقل بين Topics

#### د. اختبار البحث
- ✅ يمكن البحث في الوثائق
- ✅ النتائج تظهر بشكل صحيح
- ✅ يمكن النقر على النتائج

---

### 3. التحقق من Metadata

#### ملفات للتحقق:
1. `docs/03_TESTING/QUICK_START_TESTING_GUIDE.md`
2. `docs/05_DEVELOPMENT/MASTER_DEVELOPMENT_GUIDE.md`
3. `docs/01_CORE/STATUS/PROJECT_STATUS_DEC_2024.md`
4. `docs/08_COMMANDS/COMMAND_LIBRARY_DOCUMENTATION.md`

**التحقق من:**
- ✅ وجود metadata في بداية الملف
- ✅ YAML front matter صحيح
- ✅ جميع الحقول الإلزامية موجودة
- ✅ الروابط في `related` و `see_also` صحيحة

---

### 4. التحقق من الروابط

#### أ. روابط داخلية
- ✅ جميع الروابط النسبية تعمل
- ✅ لا توجد روابط مكسورة
- ✅ المسارات صحيحة بعد إعادة التنظيم

#### ب. روابط بين الوثائق
- ✅ `related` links تعمل
- ✅ `see_also` links تعمل
- ✅ `depends_on` links تعمل

---

## 📊 قائمة التحقق

### Backend
- [ ] `/api/v1/docs/list_files/` يعمل
- [ ] `/api/v1/docs/get_file/` يعمل
- [ ] `/api/v1/docs/search/` يعمل
- [ ] Role filtering يعمل
- [ ] Tree view يعمل
- [ ] Topics view يعمل

### Frontend
- [ ] صفحة `/docs` تفتح
- [ ] Tree view يعمل
- [ ] Topics view يعمل
- [ ] البحث يعمل
- [ ] عرض الملفات يعمل
- [ ] لا توجد أخطاء في Console

### Metadata
- [ ] الملفات المهمة لها metadata كامل
- [ ] YAML front matter صحيح
- [ ] Tags موجودة وشاملة
- [ ] Roles محددة بشكل صحيح

### الروابط
- [ ] الروابط الداخلية تعمل
- [ ] الروابط بين الوثائق تعمل
- [ ] لا توجد روابط مكسورة

---

## 🐛 المشاكل المحتملة والحلول

### المشكلة 1: 404 Not Found
**السبب:** Backend يبحث في المكان الخطأ
**الحل:** تأكد من تحديث `get_docs_path()` في `views.py`

### المشكلة 2: Metadata لا يظهر
**السبب:** YAML front matter غير صحيح
**الحل:** تحقق من صحة YAML syntax

### المشكلة 3: روابط مكسورة
**السبب:** الروابط لم يتم تحديثها
**الحل:** شغّل `update_docs_links.py` مرة أخرى

---

## ✅ النتيجة النهائية

بعد إكمال جميع الاختبارات:
- ✅ النظام يعمل بشكل صحيح
- ✅ جميع الوثائق قابلة للوصول
- ✅ البحث والفلترة تعمل
- ✅ Metadata كامل وصحيح

---

**آخر تحديث:** 06 ديسمبر 2025

